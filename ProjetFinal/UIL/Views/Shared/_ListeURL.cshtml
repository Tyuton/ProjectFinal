@model IEnumerable<WebScraper.WCF.PageContract>


@if (Model.Count() > 0)
{

    foreach (var item in Model)
    {
        <a class="list-group-item url" name="url" id="@item.Id">@item.URL</a>
    }

 } 
